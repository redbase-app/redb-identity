{
  "//": [
    "Tsak Layer 4 stub — module identity only.",
    "All business config lives in the EXTERNAL Libs/context.json (Layer 3),",
    "alongside the .tpkg files. See: redb.Identity.Core.Module.config.example.json",
    "for the full annotated template (reference, NOT shipped)."
  ],
  "ContextName": "identity.core",
  "AutoStart": true
}
