{
  "//": [
    "Tsak Layer 4 stub — module identity only.",
    "All business config lives in the EXTERNAL modules/context.json (Layer 3), alongside the .tpkg files.",
    "The WS-Trust facade reads IdentityTransport:Soap:* there; shared issuer and feature flags come from",
    "Identity:* — the same section Core, the HTTP facade and the gRPC facade read."
  ],
  "ContextName": "identity.soap",
  "AutoStart": true
}
